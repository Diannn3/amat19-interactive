---
import AppLayout from '../../layouts/AppLayout.astro';
import LabShell from '../../components/course/LabShell.astro';
import EquivalenceLab from '../../components/labs/equivalence/EquivalenceLab';
---
<AppLayout title="Equivalence Lab" description="Compare two propositions across every valuation and surface the first concrete counterexample when they differ.">
  <LabShell moduleId="logic" moduleLabel="Logic" scope="current" eyebrow="Logic · equivalence" title="Equivalence Lab" description="Compare two propositions across every valuation and surface the first concrete counterexample when they differ." lessonHref="/lessons/logic/equivalence">
    <EquivalenceLab client:load />
  </LabShell>
</AppLayout>
