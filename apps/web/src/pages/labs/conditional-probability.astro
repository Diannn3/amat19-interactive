---
import AppLayout from '../../layouts/AppLayout.astro';
import LabShell from '../../components/course/LabShell.astro';
import ConditionalProbabilityLab from '../../components/labs/conditional-probability/ConditionalProbabilityLab';
---
<AppLayout title="Conditional Probability Lab" description="See the active sample space change when conditioning, compare P(A|B) with P(B|A), and test independence exactly.">
  <LabShell moduleId="probability" moduleLabel="Probability" scope="current" eyebrow="Probability · conditioning and independence" title="Conditional Probability Lab" description="See the active sample space change when conditioning, compare P(A|B) with P(B|A), and test independence exactly." lessonHref="/lessons/probability/conditional-probability">
    <ConditionalProbabilityLab client:load />
  </LabShell>
</AppLayout>
