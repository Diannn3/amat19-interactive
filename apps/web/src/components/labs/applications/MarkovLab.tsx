import { useMemo, useState } from 'react';import { distribution,distributionAfter,matrixPower,matrixToStrings,stationaryTwoState,transitionMatrix } from '@amat19/domain-linear';import { Feedback } from '../../ui/Feedback';
export default function MarkovLab(){const[p00,setP00]=useState('3/4'),[p01,setP01]=useState('1/4'),[p10,setP10]=useState('1/2'),[p11,setP11]=useState('1/2'),[d0,setD0]=useState('1'),[d1,setD1]=useState('0'),[steps,setSteps]=useState(3);const out=useMemo(()=>{try{const P=transitionMatrix([[p00,p01],[p10,p11]]),d=distribution([d0,d1]),after=distributionAfter(d,P,steps),Pk=matrixPower(P,steps);let stationary;try{stationary=stationaryTwoState(P)}catch{}return{P,d,after,Pk,stationary,error:undefined}}catch(e){return{error:e instanceof Error?e.message:'Markov model is invalid.'}}},[p00,p01,p10,p11,d0,d1,steps]);return <section className="markov-lab" data-testid="markov-lab"><div className="markov-lab__controls"><p className="section-label">Supplemental · Markov chain</p><h2>One matrix describes how probability mass moves between states.</h2><div className="markov-matrix-editor"><label>State 1 → 1<input value={p00} onChange={e=>setP00(e.target.value)}/></label><label>State 1 → 2<input value={p01} onChange={e=>setP01(e.target.value)}/></label><label>State 2 → 1<input value={p10} onChange={e=>setP10(e.target.value)}/></label><label>State 2 → 2<input value={p11} onChange={e=>setP11(e.target.value)}/></label></div><div className="number-triple"><label className="form-field"><span className="form-field__label">Initial P(State 1)</span><input className="text-input" value={d0} onChange={e=>setD0(e.target.value)}/></label><label className="form-field"><span className="form-field__label">Initial P(State 2)</span><input className="text-input" value={d1} onChange={e=>setD1(e.target.value)}/></label><label className="form-field"><span className="form-field__label">Steps k</span><input className="text-input" type="number" min="0" step="1" value={steps} onChange={e=>setSteps(Number(e.target.value))}/></label></div>{out.error&&<Feedback tone="error">{out.error}</Feedback>}</div><div className="markov-lab__visual">{!out.error&&out.P&&<><MarkovGraph values={[[p00,p01],[p10,p11]]}/><div className="formula-callout"><span>Distribution after {steps} step{steps===1?'':'s'}</span><strong>[{out.after!.map(v=>v.toString()).join(', ')}]</strong></div>{out.stationary&&<div className="formula-callout"><span>Stationary distribution</span><strong>[{out.stationary.map(v=>v.toString()).join(', ')}]</strong><small>Starting here leaves the distribution unchanged after one transition.</small></div>}</>}</div><aside className="markov-lab__matrix"><p className="section-label">k-step transition matrix</p>{out.Pk&&<table className="matrix-table"><tbody>{matrixToStrings(out.Pk).map((r,i)=><tr key={i}>{r.map((v,j)=><td key={j}>{v}</td>)}</tr>)}</tbody></table>}<p className="experimental-note">Markov chains appear in the older Chapter 4 handout but are not explicit in the current course outline, so this route is clearly labeled supplemental.</p></aside></section>}
function MarkovGraph({values}:{values:string[][]}){return <figure className="markov-graph"><svg viewBox="0 0 600 260" role="img" aria-label="Two state Markov transition graph"><circle cx="170" cy="130" r="55"/><circle cx="430" cy="130" r="55"/><text x="170" y="135" textAnchor="middle">State 1</text><text x="430" y="135" textAnchor="middle">State 2</text><path d="M225 115 C300 65 350 65 375 115"/><path d="M375 145 C320 200 270 200 225 145"/><text x="300" y="75" textAnchor="middle">{values[0]![1]}</text><text x="300" y="210" textAnchor="middle">{values[1]![0]}</text><path d="M145 75 C70 10 70 110 120 110"/><text x="70" y="55">{values[0]![0]}</text><path d="M455 75 C530 10 530 110 480 110"/><text x="510" y="55">{values[1]![1]}</text></svg></figure>}
