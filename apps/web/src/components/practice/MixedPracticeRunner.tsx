import { useEffect, useMemo, useState } from 'react';
import { CheckCircle2, RefreshCw, ExternalLink, LockKeyhole, Sparkles, Target, History } from 'lucide-react';
import { practicePresets, skillGraph, type ModuleId } from '@amat19/course-content';
import { DexiePersistence } from '@amat19/persistence';
import { Button } from '../ui/Button';import { Feedback } from '../ui/Feedback';import { Badge } from '../ui/Badge';
import { generateMixedAssessment, type AssessmentExercise, type AssessmentModule } from '../../lib/mixed-assessment';
import { recordAttempt, recordSkillEvidence } from '../../lib/local-progress';
type Mode='practice'|'exam';function freshSeed(prefix:string){return`${prefix}-${Date.now()}-${Math.random().toString(36).slice(2,7)}`}
export default function MixedPracticeRunner({mode='practice',questionCount=10}:{mode?:Mode;questionCount?:number}){
 const[seed,setSeed]=useState(()=>freshSeed(mode));const[presetId,setPresetId]=useState(mode==='exam'?'exam-mix':'quick-5');const[modules,setModules]=useState<AssessmentModule[]>();const[skillId,setSkillId]=useState<string>();const[adaptiveCount,setAdaptiveCount]=useState(questionCount);const[answers,setAnswers]=useState<Record<string,number>>({});const[checked,setChecked]=useState<Record<string,boolean>>({});const[submitted,setSubmitted]=useState(false);const[saved,setSaved]=useState(false);
 useEffect(()=>{if(mode==='exam')return;const params=new URLSearchParams(window.location.search);const requested=params.get('preset');const requestedSkill=params.get('skill')||undefined;if(requested&&practicePresets.some(p=>p.id===requested))setPresetId(requested);if(requestedSkill){setSkillId(requestedSkill);const node=skillGraph.find(s=>s.id===requestedSkill);if(node)setModules([node.module]);}},[mode]);
 useEffect(()=>{if(mode==='exam')return;const preset=practicePresets.find(p=>p.id===presetId);if(!preset)return;setAdaptiveCount(preset.count);if(preset.modules)setModules(preset.modules as AssessmentModule[]);else if(preset.adaptive==='none'||preset.adaptive==='review')setModules(undefined);if(preset.adaptive==='weak'||preset.adaptive==='recent-mistakes'){const db=new DexiePersistence();Promise.all([db.listMastery(),db.listAttempts()]).then(([mastery,attempts])=>{if(preset.adaptive==='recent-mistakes'){const recent=[...new Set(attempts.filter(a=>a.finalState==='incomplete').slice(0,12).map(a=>a.module as AssessmentModule))];setModules(recent.length?recent:undefined);return;}const moduleScores=new Map<ModuleId,{sum:number,count:number}>();for(const record of mastery){const node=skillGraph.find(s=>s.id===record.skillId||s.parentId===record.skillId);if(!node)continue;const current=moduleScores.get(node.module)??{sum:0,count:0};current.sum+=record.evidenceScore;current.count+=1;moduleScores.set(node.module,current)}const ranked=[...moduleScores.entries()].sort((a,b)=>(a[1].count?a[1].sum/a[1].count:0)-(b[1].count?b[1].sum/b[1].count:0));setModules(ranked.length?[ranked[0]![0] as AssessmentModule]:undefined)}).catch(()=>setModules(undefined));}},[presetId,mode]);
 const questions=useMemo(()=>generateMixedAssessment(seed,mode==='exam'?questionCount:adaptiveCount,{modules,skillId}),[seed,mode,questionCount,adaptiveCount,modules?.join('|'),skillId]);const score=questions.reduce((total,q)=>total+(answers[q.id]===q.correctIndex?1:0),0);
 function choosePreset(id:string){setPresetId(id);setSkillId(undefined);setSeed(freshSeed(id));setAnswers({});setChecked({});setSubmitted(false);setSaved(false)}
 async function checkOne(question:AssessmentExercise){setChecked(c=>({...c,[question.id]:true}));const selected=answers[question.id];if(selected===undefined)return;const correct=selected===question.correctIndex;await Promise.all([recordAttempt({prefix:'mixed-practice',exerciseId:question.id,module:question.module,finalState:correct?'correct':'incomplete',payload:{seed,questionId:question.id,selected,correctIndex:question.correctIndex,mode,presetId},skillIds:[question.skillId]}),recordSkillEvidence(question.skillId,correct?1:0,{independent:correct})]).catch(()=>undefined)}
 async function submitExam(){setSubmitted(true);if(saved)return;setSaved(true);await Promise.all(questions.map(async q=>{const selected=answers[q.id];const correct=selected===q.correctIndex;await recordAttempt({prefix:'mixed-check',exerciseId:q.id,module:q.module,finalState:correct?'correct':'incomplete',payload:{seed,questionId:q.id,selected,correctIndex:q.correctIndex,mode:'exam'},skillIds:[q.skillId]}).catch(()=>undefined);await recordSkillEvidence(q.skillId, correct ? 0.9 : 0, { independent: correct }).catch(()=>undefined)}))}
 function reset(){setSeed(freshSeed(mode));setAnswers({});setChecked({});setSubmitted(false);setSaved(false)}
 return <div className="mixed-practice" data-testid={mode==='exam'?'mixed-exam':'mixed-practice'}>{mode==='practice'&&<div className="practice-presets" aria-label="Practice presets">{practicePresets.filter(p=>p.id!=='exam-mix').map(preset=><Button key={preset.id} variant={preset.id===presetId?'primary':'secondary'} onClick={()=>choosePreset(preset.id)}>{preset.id==='weak-areas'?<Target size={15}/>:preset.id==='recent-mistakes'?<History size={15}/>:<Sparkles size={15}/>} {preset.label}</Button>)}</div>}<div className="mixed-practice__toolbar"><div><p className="eyebrow">Original generated set · seed {seed}</p><h2>{mode==='exam'?'Mixed course check':practicePresets.find(p=>p.id===presetId)?.label??'Mixed practice'}</h2><p>{mode==='exam'?'Feedback stays hidden until submission. This is a study check, not an official course examination.':practicePresets.find(p=>p.id===presetId)?.description??'Check each item, read the reason, then repair the linked skill.'}</p>{modules?.length&&<div className="hero-stage__meta">{modules.map(module=><Badge key={module}>{module}</Badge>)}</div>}</div><Button variant="secondary" type="button" onClick={reset}><RefreshCw size={16}/> New set</Button></div><ol className="mixed-question-list">{questions.map((question,index)=>{const showResult=mode==='exam'?submitted:checked[question.id];const selected=answers[question.id];const correct=selected===question.correctIndex;return <li className="mixed-question" key={question.id} data-result={showResult?(correct?'correct':'wrong'):undefined}><div className="mixed-question__head"><Badge>{question.module}</Badge><strong>{index+1}. {question.title}</strong></div><p>{question.prompt}</p><fieldset disabled={mode==='exam'&&submitted}><legend className="sr-only">Answer question {index+1}</legend><div className="choice-grid">{question.choices.map((choice,choiceIndex)=><label className="choice-option" key={choice}><input type="radio" name={question.id} checked={selected===choiceIndex} onChange={()=>setAnswers(c=>({...c,[question.id]:choiceIndex}))}/><span>{choice}</span></label>)}</div></fieldset>{mode==='practice'&&!showResult&&<Button variant="primary" type="button" disabled={selected===undefined} onClick={()=>void checkOne(question)}>Check item</Button>}{showResult&&<div className="mixed-question__result"><Feedback tone={correct?'success':'error'}>{correct?'Correct.':`Not yet. The correct answer is ${question.choices[question.correctIndex]}.`} {question.explanation}</Feedback><a className="text-link" href={question.labHref}>Repair this skill in its lab <ExternalLink size={14}/></a></div>}</li>})}</ol>{mode==='exam'&&!submitted&&<Button variant="primary" type="button" onClick={()=>void submitExam()}><LockKeyhole size={16}/> Submit all answers</Button>}{mode==='exam'&&submitted&&<div className="mixed-practice__score" role="status"><CheckCircle2/><strong>{score}/{questions.length}</strong><span>{score===questions.length?'All items correct.':'Use the repair links for missed concepts, then generate a new set.'}</span></div>}</div>
}
