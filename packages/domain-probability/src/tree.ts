import { Rational, sumRationals, type RationalLike } from '@amat19/math-core';
export type ProbabilityBranch={id:string;label:string;probability:RationalLike;children?:ProbabilityBranch[]};
export type ProbabilityPath={ids:string[];labels:string[];probability:Rational};
function validateBranch(branch:ProbabilityBranch):void{const p=Rational.from(branch.probability);if(!p.isProbability())throw new RangeError(`Branch ${branch.label} must have probability between 0 and 1.`);if(branch.children?.length){const sum=sumRationals(branch.children.map(child=>child.probability));if(!sum.equals(1))throw new RangeError(`Children of ${branch.label} must sum exactly to 1; received ${sum}.`);branch.children.forEach(validateBranch)}}
export function validateProbabilityTree(branches:ProbabilityBranch[]):void{if(!branches.length)throw new RangeError('A probability tree needs at least one first-stage branch.');const sum=sumRationals(branches.map(branch=>branch.probability));if(!sum.equals(1))throw new RangeError(`First-stage probabilities must sum exactly to 1; received ${sum}.`);branches.forEach(validateBranch)}
export function enumerateProbabilityPaths(branches:ProbabilityBranch[]):ProbabilityPath[]{validateProbabilityTree(branches);const out:ProbabilityPath[]=[];function walk(branch:ProbabilityBranch,ids:string[],labels:string[],probability:Rational){const next=probability.multiply(branch.probability),nextIds=[...ids,branch.id],nextLabels=[...labels,branch.label];if(branch.children?.length)branch.children.forEach(child=>walk(child,nextIds,nextLabels,next));else out.push({ids:nextIds,labels:nextLabels,probability:next})}branches.forEach(branch=>walk(branch,[],[],Rational.one()));return out}
export function probabilityOfPaths(branches:ProbabilityBranch[],predicate:(path:ProbabilityPath)=>boolean):Rational{return sumRationals(enumerateProbabilityPaths(branches).filter(predicate).map(path=>path.probability))}
