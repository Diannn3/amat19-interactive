import type { AnnuityTiming, Cashflow, FinanceResult, FinanceTraceStep, ValueDirection } from './types.ts';
import { FinanceDecimal as D } from './decimal.ts';
function finite(name:string,value:number){if(!Number.isFinite(value))throw new RangeError(`${name} must be finite.`)}function nonnegative(name:string,value:number){finite(name,value);if(value<0)throw new RangeError(`${name} cannot be negative.`)}function validRate(rate:number){finite('Rate',rate);if(rate<=-1)throw new RangeError('The effective rate must be greater than -100%.')}function validFrequency(m:number){if(!Number.isInteger(m)||m<=0)throw new RangeError('Compounding frequency must be a positive integer.')}
const result=(value:D,trace:FinanceTraceStep[]):FinanceResult=>({value:value.toNumber(),exactValue:value.toString(),trace});
export function roundFinance(value:number,places=4):number{const factor=10**places;return Math.round((value+Number.EPSILON)*factor)/factor}
export function formatFinanceExact(value:FinanceResult|D,places=12):string{return value instanceof D?value.toString(places):D.from(value.exactValue).toString(places)}
export function simpleAccumulation(principal:number,annualRate:number,years:number):FinanceResult{nonnegative('Principal',principal);validRate(annualRate);nonnegative('Time',years);const p=D.from(principal),i=D.from(annualRate),t=D.from(years),factor=D.one().add(i.multiply(t)),value=p.multiply(factor);return result(value,[{id:'identify',label:'Identify the simple-interest model',expression:'A = P(1 + it)',explanation:'Simple interest is earned only on the original principal.'},{id:'factor',label:'Build the accumulation factor',expression:`1 + (${annualRate})(${years}) = ${factor.toString(12)}`,explanation:'Multiply the annual rate by elapsed years, then add 1.',value:factor.toNumber(),exactValue:factor.toString()},{id:'accumulate',label:'Accumulate the principal',expression:`${principal}(${factor.toString(12)}) = ${value.toString(12)}`,explanation:'Scale the original principal by the simple-interest accumulation factor.',value:value.toNumber(),exactValue:value.toString()}])}
export function compoundAccumulation(principal:number,annualEffectiveRate:number,years:number):FinanceResult{nonnegative('Principal',principal);validRate(annualEffectiveRate);nonnegative('Time',years);const p=D.from(principal),base=D.one().add(annualEffectiveRate),factor=base.powRational(years),value=p.multiply(factor);return result(value,[{id:'identify',label:'Identify the compound model',expression:'A = P(1 + i)^t',explanation:'Compound interest earns interest on the accumulated balance.'},{id:'factor',label:'Build the accumulation factor',expression:`(1 + ${annualEffectiveRate})^${years} = ${factor.toString(12)}`,explanation:'Raise the one-period accumulation factor to the elapsed number of periods.',value:factor.toNumber(),exactValue:factor.toString()},{id:'accumulate',label:'Accumulate the principal',expression:`${principal}(${factor.toString(12)}) = ${value.toString(12)}`,explanation:'Multiply principal by the compound accumulation factor.',value:value.toNumber(),exactValue:value.toString()}])}
export function nominalAccumulation(principal:number,nominalAnnualRate:number,m:number,years:number):FinanceResult{nonnegative('Principal',principal);finite('Nominal rate',nominalAnnualRate);validFrequency(m);nonnegative('Time',years);const periodic=D.from(nominalAnnualRate).divide(m);if(D.one().add(periodic).compare(0)<=0)throw new RangeError('The rate per conversion period must keep the accumulation factor positive.');const periods=D.from(m).multiply(years),factor=D.one().add(periodic).powRational(periods.toNumber()),value=D.from(principal).multiply(factor);return result(value,[{id:'periodic',label:'Convert the nominal rate to a periodic rate',expression:`j/m = ${nominalAnnualRate}/${m} = ${periodic.toString(12)}`,explanation:'A nominal annual rate is divided by its number of conversion periods.',value:periodic.toNumber(),exactValue:periodic.toString()},{id:'periods',label:'Count conversion periods',expression:`mt = ${m}(${years}) = ${periods.toString(8)}`,explanation:'Compounding occurs once per conversion period.',value:periods.toNumber(),exactValue:periods.toString()},{id:'accumulate',label:'Compound over all conversion periods',expression:`A = ${principal}(1 + ${periodic.toString(12)})^${periods.toString(8)} = ${value.toString(12)}`,explanation:'Use the periodic rate for every conversion period.',value:value.toNumber(),exactValue:value.toString()}])}
export function nominalToEffective(nominalAnnualRate:number,m:number):FinanceResult{finite('Nominal rate',nominalAnnualRate);validFrequency(m);const periodic=D.from(nominalAnnualRate).divide(m);if(D.one().add(periodic).compare(0)<=0)throw new RangeError('The periodic accumulation factor must be positive.');const value=D.one().add(periodic).powInt(m).subtract(1);return result(value,[{id:'periodic',label:'Periodic rate',expression:`j/m = ${nominalAnnualRate}/${m} = ${periodic.toString(12)}`,explanation:'Divide the quoted nominal annual rate by the conversion frequency.',value:periodic.toNumber(),exactValue:periodic.toString()},{id:'annualize',label:'Equivalent annual effective rate',expression:`i = (1 + ${periodic.toString(12)})^${m} - 1 = ${value.toString(12)}`,explanation:'Equivalent rates produce the same one-year accumulation.',value:value.toNumber(),exactValue:value.toString()}])}
export function effectiveToNominal(annualEffectiveRate:number,m:number):FinanceResult{validRate(annualEffectiveRate);validFrequency(m);const periodic=D.one().add(annualEffectiveRate).rootInt(m).subtract(1),value=periodic.multiply(m);return result(value,[{id:'periodic',label:'Equivalent periodic rate',expression:`(1 + i)^(1/${m}) - 1 = ${periodic.toString(12)}`,explanation:'Find the conversion-period rate that reproduces the same annual accumulation.',value:periodic.toNumber(),exactValue:periodic.toString()},{id:'quote',label:'Nominal annual quote',expression:`j = ${m}(${periodic.toString(12)}) = ${value.toString(12)}`,explanation:'Multiply the periodic rate by the number of conversion periods.',value:value.toNumber(),exactValue:value.toString()}])}
export function valueAtTime(amount:number,fromTime:number,toTime:number,annualEffectiveRate:number):FinanceResult{finite('Amount',amount);finite('Starting time',fromTime);finite('Valuation time',toTime);validRate(annualEffectiveRate);const periods=toTime-fromTime,factor=D.one().add(annualEffectiveRate).powRational(periods),value=D.from(amount).multiply(factor),direction=periods>=0?'accumulate':'discount';return result(value,[{id:'distance',label:'Measure the time shift',expression:`${toTime} - ${fromTime} = ${periods}`,explanation:'Cash flows must be moved to one common focal date before they can be compared.',value:periods},{id:'move',label:direction==='accumulate'?'Accumulate forward':'Discount backward',expression:`${amount}(1 + ${annualEffectiveRate})^${periods} = ${value.toString(12)}`,explanation:direction==='accumulate'?'Moving right on the timeline multiplies by accumulation factors.':'Moving left on the timeline divides by accumulation factors.',value:value.toNumber(),exactValue:value.toString()}])}
export function valueCashflowsAt(cashflows:Cashflow[],valuationTime:number,annualEffectiveRate:number):FinanceResult{if(cashflows.length===0)throw new RangeError('At least one cash flow is required.');validRate(annualEffectiveRate);finite('Valuation time',valuationTime);const trace:FinanceTraceStep[]=[];let value=D.zero();cashflows.forEach((flow,index)=>{finite('Cash-flow time',flow.time);finite('Cash-flow amount',flow.amount);const moved=D.from(flow.amount).multiply(D.one().add(annualEffectiveRate).powRational(valuationTime-flow.time));value=value.add(moved);trace.push({id:`flow-${index}`,label:flow.label||`Cash flow ${index+1}`,expression:`${flow.amount}(1 + ${annualEffectiveRate})^(${valuationTime} - ${flow.time}) = ${moved.toString(12)}`,explanation:'Move this cash flow to the common focal date before adding it.',value:moved.toNumber(),exactValue:moved.toString()})});trace.push({id:'sum',label:'Combine values at the focal date',expression:`V(${valuationTime}) = ${value.toString(12)}`,explanation:'Amounts may be added only after they refer to the same valuation date.',value:value.toNumber(),exactValue:value.toString()});return result(value,trace)}
function annuityFactor(rate:number,n:number,direction:ValueDirection):D{validRate(rate);if(!Number.isInteger(n)||n<0)throw new RangeError('The number of payments must be a nonnegative integer.');if(n===0)return D.zero();if(rate===0)return D.from(n);const base=D.one().add(rate);return direction==='present'?D.one().subtract(base.powInt(-n)).divide(rate):base.powInt(n).subtract(1).divide(rate)}
export function annuityValue(payment:number,ratePerPaymentPeriod:number,n:number,timing:AnnuityTiming,direction:ValueDirection):FinanceResult{finite('Payment',payment);const base=annuityFactor(ratePerPaymentPeriod,n,direction),timingFactor=timing==='due'?D.one().add(ratePerPaymentPeriod):D.one(),unit=base.multiply(timingFactor),value=D.from(payment).multiply(unit),symbol=direction==='present'?'a-angle-n':'s-angle-n',baseExpr=ratePerPaymentPeriod===0?`${n}`:direction==='present'?'(1 - (1+i)^(-n))/i':'((1+i)^n - 1)/i';return result(value,[{id:'base',label:`${direction==='present'?'Present':'Future'}-value annuity factor`,expression:`${symbol} = ${baseExpr} = ${base.toString(12)}`,explanation:`This factor combines ${n} equal ${timing==='immediate'?'end-of-period':'beginning-of-period'} payments at one focal date.`,value:base.toNumber(),exactValue:base.toString()},...(timing==='due'?[{id:'due',label:'Shift an annuity-immediate one period',expression:`factor × (1 + i) = ${unit.toString(12)}`,explanation:'Every annuity-due payment occurs one period earlier, so the entire value is one accumulation factor larger.',value:unit.toNumber(),exactValue:unit.toString()}]:[]),{id:'payment',label:'Scale by the payment amount',expression:`${payment} × ${unit.toString(12)} = ${value.toString(12)}`,explanation:'The annuity factor is the value of unit payments; multiply by the actual payment.',value:value.toNumber(),exactValue:value.toString()}])}
export function annuityPaymentForValue(targetValue:number,rate:number,n:number,timing:AnnuityTiming,direction:ValueDirection):FinanceResult{finite('Target value',targetValue);const unit=annuityValue(1,rate,n,timing,direction);if(unit.value===0)throw new RangeError('The annuity factor is zero, so a payment cannot be solved.');const value=D.from(targetValue).divide(unit.exactValue);return result(value,[...unit.trace,{id:'solve-payment',label:'Solve for the level payment',expression:`R = ${targetValue} / ${unit.exactValue} = ${value.toString(12)}`,explanation:'Divide the target value by the value of one unit payment.',value:value.toNumber(),exactValue:value.toString()}])}
export type BondPriceResult=FinanceResult&{couponPayment:number;redemptionPresentValue:number;couponPresentValue:number;classification:'premium'|'discount'|'par'};
export function bondPrice(input:{faceValue:number;couponRatePerPeriod:number;redemptionValue:number;yieldPerPeriod:number;periods:number}):BondPriceResult{const{faceValue,couponRatePerPeriod,redemptionValue,yieldPerPeriod,periods}=input;nonnegative('Face value',faceValue);nonnegative('Redemption value',redemptionValue);finite('Coupon rate',couponRatePerPeriod);validRate(yieldPerPeriod);if(!Number.isInteger(periods)||periods<=0)throw new RangeError('Coupon periods must be a positive integer.');const couponPayment=D.from(faceValue).multiply(couponRatePerPeriod),discount=D.one().add(yieldPerPeriod).powInt(-periods),factor=yieldPerPeriod===0?D.from(periods):D.one().subtract(discount).divide(yieldPerPeriod),couponPV=couponPayment.multiply(factor),redemptionPV=D.from(redemptionValue).multiply(discount),value=couponPV.add(redemptionPV),tolerance=Math.max(1,Math.abs(redemptionValue))*1e-10,classification=value.toNumber()>redemptionValue+tolerance?'premium':value.toNumber()<redemptionValue-tolerance?'discount':'par';return{...result(value,[{id:'coupon',label:'Find the coupon payment',expression:`Fr = ${faceValue}(${couponRatePerPeriod}) = ${couponPayment.toString(12)}`,explanation:'The face value and coupon rate per coupon period determine each level coupon.',value:couponPayment.toNumber(),exactValue:couponPayment.toString()},{id:'coupons',label:'Value the coupon stream',expression:`(Fr)a_n|j = ${couponPayment.toString(12)}(${factor.toString(12)}) = ${couponPV.toString(12)}`,explanation:'The coupons form an annuity-immediate valued at the bond yield per coupon period.',value:couponPV.toNumber(),exactValue:couponPV.toString()},{id:'redemption',label:'Discount the redemption value',expression:`Cv^n = ${redemptionValue}(1 + ${yieldPerPeriod})^(-${periods}) = ${redemptionPV.toString(12)}`,explanation:'The redemption amount is a single future cash flow at maturity.',value:redemptionPV.toNumber(),exactValue:redemptionPV.toString()},{id:'price',label:'Combine all values at the purchase date',expression:`P = ${couponPV.toString(12)} + ${redemptionPV.toString(12)} = ${value.toString(12)}`,explanation:'Bond price is the present value of all coupons plus the present value of redemption.',value:value.toNumber(),exactValue:value.toString()}]),couponPayment:couponPayment.toNumber(),couponPresentValue:couponPV.toNumber(),redemptionPresentValue:redemptionPV.toNumber(),classification}}
