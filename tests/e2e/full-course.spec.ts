import AxeBuilder from '@axe-core/playwright'; import { expect, test } from '@playwright/test';
const routes=['/modules/finance','/modules/linear','/modules/applications','/labs/interest','/labs/annuity','/labs/bonds','/labs/matrix-operations','/labs/row-reduction','/labs/linear-programming','/labs/game-theory','/labs/probability-simulation'];
const labRoutes=['/labs/annuity','/labs/bayes','/labs/bonds','/labs/cashflow-timeline','/labs/conditional-probability','/labs/counting','/labs/distribution','/labs/equivalence','/labs/formal-proof','/labs/game-theory','/labs/interest','/labs/linear-programming','/labs/logic-basics','/labs/markov','/labs/matrix-operations','/labs/probability-simulation','/labs/row-reduction','/labs/truth-table'];

test('@core every lab uses the shared context shell',async({page})=>{for(const route of labRoutes){await page.goto(route);await expect(page.locator('[data-lab-shell]'),route).toHaveCount(1);await expect(page.locator('.lab-route__workspace'),route).toBeVisible();await expect(page.getByRole('navigation',{name:'Breadcrumb'}).getByRole('link',{name:'Home'}),route).toBeVisible();}});
test('@core lab routes emit no page or console errors',async({page})=>{const errors:string[]=[];let currentRoute='';page.on('pageerror',error=>errors.push(`${currentRoute} pageerror: ${error.message}`));page.on('console',message=>{if(message.type()==='error')errors.push(`${currentRoute} console: ${message.text()}`)});for(const route of labRoutes){currentRoute=route;await page.goto(route);const islandCount=await page.locator('astro-island').count();await expect(page.locator('astro-island[client-render-time]'),route).toHaveCount(islandCount);await expect(page.locator('main'),route).toBeVisible();}expect(errors).toEqual([]);});
test('@core audited surfaces have no axe accessibility violations',async({page})=>{test.setTimeout(120_000);const routes=['/progress','/labs/conditional-probability','/labs/distribution','/labs/game-theory','/labs/matrix-operations','/labs/row-reduction'];for(const route of routes){await page.goto(route);const islandCount=await page.locator('astro-island[client="load"]').count();if(islandCount)await expect(page.locator('astro-island[client="load"][client-render-time]'),route).toHaveCount(islandCount);const results=await new AxeBuilder({page}).analyze();expect(results.violations,route).toEqual([]);}});
for(const route of routes){test(`@core ${route} renders without page overflow`,async({page})=>{await page.goto(route);await expect(page.locator('main')).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true);});}
test('@core finance compound model updates deterministic result',async({page})=>{await page.goto('/labs/interest');const lab=page.getByTestId('interest-lab');await expect(lab).toBeVisible();await expect(lab.getByText(/Accumulated value/)).toBeVisible();});
test('@core annuity timing can switch to due',async({page})=>{await page.goto('/labs/annuity');const lab=page.getByTestId('annuity-lab');await expect(lab).toHaveAttribute('data-hydrated','true');await lab.getByRole('button',{name:/Due/}).click();await expect(lab.getByText(/beginning of each period/i)).toBeVisible();});
test('@core matrix multiplication exposes selected dot product',async({page})=>{await page.goto('/labs/matrix-operations');const lab=page.getByTestId('matrix-operations-lab');await expect(lab.getByText(/Row 1 · Column 1/)).toBeVisible();await expect(lab.getByText(/Sum =/)).toBeVisible();});
test('@core row reduction exposes unique solution',async({page})=>{await page.goto('/labs/row-reduction');const lab=page.getByTestId('row-reduction-lab');await expect(lab.locator('.formula-callout').getByText('unique',{exact:true})).toBeVisible();});
test('@core formal proof primary action remains a usable mobile control',async({page})=>{await page.goto('/labs/formal-proof');const islandCount=await page.locator('astro-island[client="load"]').count();if(islandCount)await expect(page.locator('astro-island[client="load"][client-render-time]')).toHaveCount(islandCount);const action=page.getByRole('button',{name:'Add checked line',exact:true});await expect(action).toBeVisible();const bounds=await action.boundingBox();expect(bounds).not.toBeNull();expect(bounds!.width).toBeGreaterThanOrEqual(120);expect(bounds!.height).toBeLessThanOrEqual(56);});
test('@core shared form and disclosure controls meet the 44px interaction contract',async({page})=>{const waitForClientLoad=async()=>{const islandCount=await page.locator('astro-island[client="load"]').count();if(islandCount)await expect(page.locator('astro-island[client="load"][client-render-time]')).toHaveCount(islandCount);};const assertTargets=async(selector:string,route:string)=>{const controls=page.locator(selector);await expect(controls.first(),`${route} should render ${selector}`).toBeVisible();for(let index=0;index<await controls.count();index+=1){const bounds=await controls.nth(index).boundingBox();expect(bounds,`${route} ${selector} #${index+1}`).not.toBeNull();expect(bounds!.width,`${route} ${selector} #${index+1} width`).toBeGreaterThanOrEqual(44);expect(bounds!.height,`${route} ${selector} #${index+1} height`).toBeGreaterThanOrEqual(44);}};await page.goto('/course');await assertTargets('.route-note > summary','/course');await assertTargets('.roadmap-details > summary','/course');await assertTargets('.course-policy > details > summary','/course');await page.goto('/labs/truth-table');await waitForClientLoad();await assertTargets('.lab-route__support details > summary','/labs/truth-table');await page.goto('/labs/logic-basics');await waitForClientLoad();await assertTargets('.reference-details > summary','/labs/logic-basics');await page.goto('/labs/formal-proof');await waitForClientLoad();await assertTargets('.proof-reference .rule-grid details > summary','/labs/formal-proof');await page.goto('/labs/distribution');await waitForClientLoad();await assertTargets('.distribution-table input','/labs/distribution');await page.goto('/labs/linear-programming');await waitForClientLoad();await assertTargets('.constraint-row input, .constraint-row select','/labs/linear-programming');await page.goto('/labs/game-theory');await waitForClientLoad();await assertTargets('.payoff-table input','/labs/game-theory');await page.goto('/labs/markov');await waitForClientLoad();await assertTargets('.markov-matrix-editor input','/labs/markov');await assertTargets('.markov-stepper input[type="range"]','/labs/markov');await page.goto('/labs/row-reduction');await waitForClientLoad();await page.getByRole('button',{name:'Do the row operations',exact:true}).click();await assertTargets('.row-op-builder__controls input, .row-op-builder__controls select','/labs/row-reduction');});
test('@core graphical LP and simplex agree on sample optimum',async({page})=>{await page.goto('/labs/linear-programming');const lab=page.getByTestId('lp-lab');await expect(lab.locator('.lp-lab__visual .formula-callout strong')).toHaveText('optimal');await expect(lab.getByText(/Z = 11/)).toBeVisible();});
test('@core game theory default requires mixed strategy',async({page})=>{await page.goto('/labs/game-theory');const lab=page.getByTestId('game-theory-lab');await expect(lab.getByText(/Row mix/)).toBeVisible();});
test('all lab routes have no serious axe findings',async({page})=>{test.setTimeout(120_000);for(const route of labRoutes){await page.goto(route);const results=await new AxeBuilder({page}).analyze();expect(results.violations.filter(v=>['critical','serious'].includes(v.impact??'')),route).toEqual([]);}});
test('@core secondary lab navigation and matrix disclosures meet the 44px interaction contract',async({page})=>{const waitForClientLoad=async()=>{const islandCount=await page.locator('astro-island[client="load"]').count();if(islandCount)await expect(page.locator('astro-island[client="load"][client-render-time]')).toHaveCount(islandCount);};const assertTargets=async(selector:string,route:string)=>{const controls=page.locator(selector);await expect(controls.first(),`${route} should render ${selector}`).toBeVisible();for(let index=0;index<await controls.count();index+=1){const bounds=await controls.nth(index).boundingBox();expect(bounds,`${route} ${selector} #${index+1}`).not.toBeNull();expect(bounds!.width,`${route} ${selector} #${index+1} width`).toBeGreaterThanOrEqual(44);expect(bounds!.height,`${route} ${selector} #${index+1} height`).toBeGreaterThanOrEqual(44);}};await page.goto('/labs/matrix-operations');await waitForClientLoad();await assertTargets('.lab-route__context-rail nav a','/labs/matrix-operations');await assertTargets('.matrix-editor > details > summary','/labs/matrix-operations');await page.goto('/labs/truth-table');await waitForClientLoad();await assertTargets('.lab-route__context-rail nav a','/labs/truth-table');await assertTargets('.no-js-reference > summary','/labs/truth-table');});
test('@core dense lab editors contain their content at workspace widths',async({page})=>{const assertContained=async(route:string,selector:string)=>{await page.goto(route);const panel=page.locator(selector);await expect(panel,`${route} should render ${selector}`).toBeVisible();const metrics=await panel.evaluate((element)=>({clientWidth:element.clientWidth,scrollWidth:element.scrollWidth}));expect(metrics.scrollWidth,`${route} ${selector} scroll width`).toBeLessThanOrEqual(metrics.clientWidth+1);};await assertContained('/labs/cashflow-timeline','.finance-lab__controls');await assertContained('/labs/linear-programming','.lp-lab__controls');await assertContained('/labs/game-theory','.game-lab__matrix');});
test('@core cash-flow timeline separates coincident point labels',async({page})=>{await page.goto('/labs/cashflow-timeline');const collisions=await page.locator('.finance-timeline svg g').evaluateAll((groups)=>{const entries=groups.map((group)=>{const circle=group.querySelector('circle');const labels=Array.from(group.querySelectorAll('text')).map((text)=>text.getBoundingClientRect());if(!circle)return null;const circleBox=circle.getBoundingClientRect();return{cx:circleBox.left+circleBox.width/2,labels}}).filter((entry):entry is {cx:number;labels:DOMRect[]}=>entry!==null);const result:string[]=[];for(let left=0;left<entries.length;left+=1){for(let right=left+1;right<entries.length;right+=1){if(Math.abs(entries[left]!.cx-entries[right]!.cx)>1)continue;for(const first of entries[left]!.labels)for(const second of entries[right]!.labels)if(first.left<second.right&&first.right>second.left&&first.top<second.bottom&&first.bottom>second.top)result.push(`${left}:${right}`);}}return result;});expect(collisions).toEqual([]);});
